using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerSkill : MonoBehaviour
{
/*    public OptionSkill optionSkill;
    public string[] playerSkill = new string[3];
    Skills[]

    [Header("Skill Player")]
    public Text[] skillUIPlayer = new Text[3];

    // Start is called before the first frame update
    void Start()
    {
        optionSkill = FindObjectOfType<OptionSkill>();
        
    }

    // Update is called once per frame
    void Update()
    {

        playerSkill = optionSkill.skillPlayer;
    }

    public void playerSkillUI()
    {
        for(int i = 0; i < playerSkill.Length; i++)
        {
            skillUIPlayer[i].text = optionSkill.allSkill[1].Name;
        }
    }*/
}
